import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // allowedDevOrigins: ["http://localhost:3100", "https://asistente-all.scn.quest"],
  // allowedDevOrigins: ["*"],
};

export default nextConfig;
